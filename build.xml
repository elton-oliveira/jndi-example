<project name="agenda" xmlns:ivy="antlib:org.apache.ivy.ant" default="atualiza">

	<path id="ivy.lib.path">
		<fileset dir="ivy-lib" includes="*.jar" />
	</path>
	<taskdef resource="org/apache/ivy/ant/antlib.xml" uri="antlib:org.apache.ivy.ant" classpathref="ivy.lib.path" />

	<target name="limpar">
		<delete dir="lib" />
		<mkdir dir="lib" />
		<delete dir="lib-teste" />
		<mkdir dir="lib-teste" />
	</target>

	<target name="atualiza" description="resolver as dependências do projeto" depends="limpar">
		<ivy:retrieve conf="teste" pattern="lib-teste/[artifact]-[revision].[ext]"/>
		<ivy:retrieve conf="principal" pattern="lib/[artifact]-[revision].[ext]"/>
	</target>

</project>